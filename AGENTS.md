# Randall training package

When Randall asks to install or set up this repository, read BOOTSTRAP.md and follow the three stages. Stage 1 must finish before asking him to start a fresh chat; Stage 3 begins in that fresh chat after discovery checks.

For work on this package, preserve existing changes and use relevant skills. The portable runtime agreement is runtime.md. Never write personal profile, hardware, history, credential, or real network evidence into this distributable repository. Use the external personal owner named by the installed local pointer.

Astra supervises; explicitly assign substantial bounded work to gpt-5.6-sol when the runtime exposes it. A role name is not a model switch. Claude is a bounded collaborator, not another recursive supervisor. Each writer owns separate files. Test installer changes in temporary homes, never in the operator's actual home.

This package cannot grant access to an employer's systems, authorize cloud ingestion of private data, or bypass platform trust/security rules.

