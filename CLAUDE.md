# Randall training package

Read AGENTS.md for this repository's entry point. For an installation request, follow BOOTSTRAP.md. Shared working behavior is in runtime.md. Preserve external personal data ownership.

If a supervising Codex task gives you a bounded work packet, do only that job and return evidence. Do not launch Codex or more supervisors recursively. Use an explicit permission mode for CLI calls; never use a bypass-permissions flag to make setup work.

